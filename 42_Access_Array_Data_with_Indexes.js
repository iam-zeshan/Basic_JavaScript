/*
I learned:
We can get/access data inside an Array by using index number 'bracket notation', same as String.

For Example:
const array = [10, 20, 40];
console.log(array[0]);
const data = array[1];
The console.log(array[0]) prints 10, and data has the value 20.

Task:
Create a variable called myData and set it to equal the first value of myArray using bracket notation.

*/ 

const myArray = [50, 60, 70];
let myData = myArray[0];
