/*
I learned:
'parseInt', this is the JavaScript built in function, in which we can convert the string value into a numbers. But if the first character wasn't the integer.

Task:
Use parseInt() in the convertToInteger function so it converts the input string str into an integer, and returns it.
*/

function convertToInteger(str) {
    return parseInt(str);
  }
  
  convertToInteger("56");